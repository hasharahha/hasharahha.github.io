var engine = document.getElementById("game").getContext("2d");
var player = {
 postion:{
   x:0,
   y:0
 },
 height: 50, width:50, health:100 }


function Keys(key){
  if(keys[])
}

function gameUpdate(){
  engine.clear();
  engine.fillRect(player.position.x, player.position.y, player.height, player.width);
}
